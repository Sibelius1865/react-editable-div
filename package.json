{
  "name": "react-input-div",
  "version": "1.2.3",
  "description": "A lightweight library for creating editable React div components with built-in functions to retrieve",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Sibelius1865/react-input-div.git"
  },
  "license": "MIT",
  "author": "Harada Taiyo",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/",
    "README.md",
    "LICENCE"
  ],
  "scripts": {
    "build:css": "cp src/style.css dist/style.css",
    "build": "tsc && npm run build:css",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/react": "^19.0.7",
    "react": "^19.0.0",
    "typescript": "^5.7.3"
  },
  "peerDependencies": {
    "react": ">=18.0.0",
    "react-dom": ">=18.0.0"
  }
}
